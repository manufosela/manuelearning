---
import MainLayout from '../layouts/MainLayout.astro';
---

<MainLayout title="Sobre Mánu | ManuElearning" description="Conoce a Mánu Fosela, desarrollador con más de 25 años de experiencia en software y formación tecnológica.">

  <!-- Hero -->
  <section class="about-hero">
    <div class="about-hero__content">
      <h2 class="section-label">Sobre Mánu</h2>
      <h1 class="about-hero__title">Mánu Fosela</h1>
      <p class="about-hero__subtitle">Desarrollador, formador y apasionado de la tecnología desde hace más de 25 años.</p>
    </div>
  </section>

  <!-- Bio -->
  <section class="about-bio">
    <div class="about-bio__grid">
      <div class="about-bio__text">
        <h3 class="about-bio__heading">¿Quién soy?</h3>
        <p>
          Soy Mánu Fosela, ingeniero de software con más de 25 años de experiencia profesional desarrollando aplicaciones web, liderando equipos técnicos y mentorizando a personas que están empezando en el mundo del desarrollo.
        </p>
        <p>
          He trabajado en proyectos de todo tipo: desde startups hasta grandes empresas, pasando por consultoras y producto propio. He visto nacer la web, crecer JavaScript y evolucionar las herramientas que usamos hoy.
        </p>
        <p>
          Esta plataforma nace de una necesidad real: compartir lo que sé de forma directa, práctica y sin rodeos, con personas cercanas que quieren aprender de verdad.
        </p>
      </div>
      <div class="about-bio__highlights">
        <div class="highlight-card">
          <span class="material-symbols-outlined">code</span>
          <div>
            <strong>+25 años</strong>
            <span>desarrollando software</span>
          </div>
        </div>
        <div class="highlight-card">
          <span class="material-symbols-outlined">group</span>
          <div>
            <strong>+15 años</strong>
            <span>mentorizando juniors</span>
          </div>
        </div>
        <div class="highlight-card">
          <span class="material-symbols-outlined">manage_accounts</span>
          <div>
            <strong>+10 años</strong>
            <span>liderando equipos</span>
          </div>
        </div>
        <div class="highlight-card">
          <span class="material-symbols-outlined">school</span>
          <div>
            <strong>Formador</strong>
            <span>en tecnologías web</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Philosophy -->
  <section class="about-philosophy">
    <div class="about-philosophy__content">
      <h3 class="about-philosophy__heading">Mi filosofía</h3>
      <div class="about-philosophy__grid">
        <div class="philosophy-card">
          <span class="material-symbols-outlined">construction</span>
          <h4>Aprender haciendo</h4>
          <p>Los cursos están basados en proyectos reales. Nada de ejemplos de juguete: construyes cosas que funcionan.</p>
        </div>
        <div class="philosophy-card">
          <span class="material-symbols-outlined">handshake</span>
          <h4>Cercanía</h4>
          <p>No soy un avatar detrás de una pantalla. Puedes preguntarme directamente y te respondo yo.</p>
        </div>
        <div class="philosophy-card">
          <span class="material-symbols-outlined">visibility</span>
          <h4>Sin filtros</h4>
          <p>Te cuento lo que funciona y lo que no, basándome en experiencia real, no en teoría de manual.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="about-cta">
    <div class="about-cta__content">
      <h3 class="about-cta__title">¿Quieres aprender conmigo?</h3>
      <p class="about-cta__text">Echa un vistazo a los cursos disponibles o solicita acceso si aún no tienes cuenta.</p>
      <div class="about-cta__actions">
        <a href="/cursos" class="btn btn--primary btn--lg">Ver cursos</a>
        <a href="/solicitar-acceso" class="btn btn--outline btn--lg">Solicitar acceso</a>
      </div>
    </div>
  </section>

</MainLayout>

<style>
  /* --- Hero --- */
  .about-hero {
    padding: 4rem var(--section-px) 3rem;
    background: linear-gradient(to right, #0f172a 0%, #1a2e05 100%);
    text-align: center;
  }

  .about-hero__content {
    max-width: var(--container-max);
    margin-inline: auto;
  }

  .about-hero__title {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 900;
    line-height: 1.1;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }

  .about-hero__subtitle {
    color: #f1f5f9;
    font-size: clamp(1rem, 2vw, 1.25rem);
    line-height: 1.6;
    opacity: 0.9;
    max-width: 600px;
    margin-inline: auto;
  }

  /* --- Bio --- */
  .about-bio {
    padding: var(--section-py) var(--section-px);
    background-color: var(--color-bg-white);
  }

  .about-bio__grid {
    max-width: var(--container-max);
    margin-inline: auto;
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: start;
  }

  @media (min-width: 1024px) {
    .about-bio__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .about-bio__heading {
    font-size: 2rem;
    font-weight: 900;
    color: var(--color-text-primary);
    margin-bottom: 1.5rem;
  }

  .about-bio__text {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .about-bio__text p {
    color: var(--color-text-secondary);
    font-size: 1.125rem;
    line-height: 1.7;
  }

  .about-bio__highlights {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .highlight-card {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    border-radius: var(--radius-xl);
    border: 2px solid var(--color-border);
    background-color: var(--color-bg-white);
    transition: all var(--transition-base);
  }

  .highlight-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-lg);
  }

  .highlight-card > .material-symbols-outlined {
    font-size: 2rem;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .highlight-card div {
    display: flex;
    flex-direction: column;
  }

  .highlight-card strong {
    font-size: 1.125rem;
    font-weight: 800;
    color: var(--color-text-primary);
  }

  .highlight-card span:not(.material-symbols-outlined) {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  /* --- Philosophy --- */
  .about-philosophy {
    padding: var(--section-py) var(--section-px);
    background-color: var(--color-bg-slate-50);
  }

  .about-philosophy__content {
    max-width: var(--container-max);
    margin-inline: auto;
  }

  .about-philosophy__heading {
    font-size: 2rem;
    font-weight: 900;
    color: var(--color-text-primary);
    text-align: center;
    margin-bottom: 3rem;
  }

  .about-philosophy__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 768px) {
    .about-philosophy__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .philosophy-card {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 2rem;
    border-radius: var(--radius-xl);
    border: 2px solid var(--color-border);
    background-color: var(--color-bg-white);
    transition: all var(--transition-base);
  }

  .philosophy-card:hover {
    box-shadow: var(--shadow-2xl);
    border-color: var(--color-primary);
  }

  .philosophy-card > .material-symbols-outlined {
    font-size: 2rem;
    color: var(--color-secondary);
  }

  .philosophy-card h4 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-text-primary);
  }

  .philosophy-card p {
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* --- CTA --- */
  .about-cta {
    padding: var(--section-py) var(--section-px);
    background: linear-gradient(to right, #0f172a 0%, #1a2e05 100%);
    text-align: center;
  }

  .about-cta__content {
    max-width: 600px;
    margin-inline: auto;
  }

  .about-cta__title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 900;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
  }

  .about-cta__text {
    color: #f1f5f9;
    font-size: 1.125rem;
    line-height: 1.6;
    opacity: 0.9;
    margin-bottom: 2rem;
  }

  .about-cta__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }
</style>
